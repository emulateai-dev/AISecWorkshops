# Prompt sending attack

This uses PyRIT’s `PromptSendingAttack` with `OpenAIChatTarget` — the same pattern as the workshop **00_prompt_sending** notebook.

1. Enter a **benign** lab objective below (harmful content is out of scope for this lab UI).
2. Click **Run for me** to send it through your configured target (`~/.pyrit`).

Ensure **Setup** credentials are saved if your target requires an API key.
