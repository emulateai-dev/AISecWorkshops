# Prompt targets (stub)

PyRIT **prompt targets** wrap APIs (OpenAI-compatible chat, Azure, etc.). Configuration often uses `OPENAI_CHAT_ENDPOINT`, `OPENAI_CHAT_KEY`, and `OPENAI_CHAT_MODEL` — align with your **Setup** tab and `~/.pyrit` files.

**Coming soon:** interactive examples.
